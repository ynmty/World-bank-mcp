{
  "name": "worldbank-procurement-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server that exposes the World Bank Procurement Notices API (search.worldbank.org/api/v2/procnotices) as tools for AI agents.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "latest",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "wrangler": "latest",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "latest"
  }
}
